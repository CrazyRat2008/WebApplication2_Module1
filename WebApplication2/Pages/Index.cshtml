@page
@model IndexModel
@{
    ViewData["Title"] = "Home page"; 
    int num = new Random().Next(0, 26);
    char let = (char)('A' + num);
}

<div class="text-center">
   <h1>@DateTime.Now.Day</h1>
    <p>Random letter: @let</p>
</div>
